RULE inject interrupted
CLASS java.util.concurrent.ThreadPoolExecutor
METHOD execute
AT ENTRY
IF TRUE
DO traceStack("On thread [" + Thread.currentThread().getName() + "] ", 10)
ENDRULE


RULE trace run in all java.util.concurrent.ThreadPoolExecutor
CLASS java.util.concurrent.ThreadPoolExecutor
METHOD runWorker
AT ENTRY
IF TRUE
DO traceStack("On thread [" + Thread.currentThread().getName() + "] ", 10)
ENDRULE